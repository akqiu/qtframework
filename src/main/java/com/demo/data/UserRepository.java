package com.demo.data;
/*
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.CrudRepository;
import org.springframework.data.repository.query.Param;

import java.util.List;

public interface UserRepository extends CrudRepository<accountinfo, Integer>
{

    public accountinfo save(accountinfo accountInfo);

    //public accountinfo findByAccountId(Integer accountId);

    public List<accountinfo> findByBalance(Integer balance);

    //@Query("from accountinfo where accountId = ?1")
    //public accountinfo findALL(Integer accountId);
}
*/